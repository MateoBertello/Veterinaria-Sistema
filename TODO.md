# TODO — Deuda técnica

Pendientes conocidos que no se abordan en la etapa donde se detectaron. Cada ítem indica origen, fix recomendado y cuándo se detectó.

| # | Origen | Descripción | Fix recomendado | Detectada en | Resolver en |
| :---- | :---- | :---- | :---- | :---- | :---- |
| DT-1 | Etapa 2 — `usuarios.service.ts` (RN-SEC5) | Al asignar/cambiar el rol a `veterinario`, tanto `crear` como `editar` hacen **`INSERT`** en `doctores` (no `UPSERT`): editar un usuario que ya tiene perfil profesional genera **doctores duplicados**. Además el alta no setea `specialty` por defecto pese a que RN-SEC5 lo pide ("especialidad por defecto"). | Agregar `UNIQUE(tenant_id, user_id)` en `doctores` (**requiere migración nueva**) y convertir el `INSERT` en `UPSERT`/`onConflict`; setear `specialty` por defecto en el alta. | Etapa 4 (al construir el ABM de Doctores). | **Resuelta (commit `9438ddd`, Etapa 9 — S4).** Migración `20260706000002_doctores_unique_tenant_user.sql`: limpieza defensiva de duplicados (repunta `horarios_doctor`/`turnos` a la fila más antigua) + `UNIQUE (tenant_id, user_id)` **plano, no índice parcial**: los NULL no chocan entre sí (doctores sin usuario siguen siendo válidos) y PostgREST necesita un constraint completo para `on_conflict`. `crear()` y `editar()` hacen UPSERT **DO NOTHING** (`ignoreDuplicates`): re-asignar el rol no pisa specialty/matrícula/available editadas a mano; `specialty` por defecto `'Clínica general'` solo en el alta. Diagnóstico previo en stack local: 0 duplicados. Integración: `tests/integration/doctores.integration.test.ts` (23505 + upsert + NULLs). |
| DT-2 | Etapa 5 — modelo `doctores` vs `usuarios` | **Doble identidad del "profesional" no reconciliada.** Historial (`historial_clinico.professional_id`), RPC de eutanasia y RPC de vacunación referencian/validan `usuarios(id)`; en cambio Turnos/Horarios (`horarios_doctor.doctor_id`) y el endpoint `GET /doctores` giran sobre `doctores(id)`. El endpoint `/doctores` expone **ambas** claves en el mismo objeto (`id` = doctor, `userId` = usuario) y cada consumidor debe elegir la correcta a mano — origen directo del bug de FK `historial_clinico_professional_id_fkey`. | Decidir una **única fuente de verdad** del "profesional" y unificar el contrato (p. ej. que el listado que alimenta selects de historial exponga explícitamente el `usuarios.id`, o que la FK pase a `doctores`). Documentar la semántica de cada clave. | Etapa 5 (registro de evento clínico). | **Resuelta (commit `d19381d`, Etapa 9 — S4).** Contrato elegido: la FK y los RPCs **siguen en `usuarios(id)`** (cero migración de datos). Semántica documentada en `DoctorPublico` (backend) y `Doctor` (`web/src/types`): `id` = PK de `doctores` → Turnos/Horarios; `userId` = `usuarios.id` → `professionalId` de historial/vacunación. El backend expone los seleccionables vía `GET /doctores?professional=true` (filtra `user_id IS NOT NULL` server-side) y `crearRegistro` valida que el `professionalId` pertenezca al tenant → `403 FORBIDDEN` (antes: 500 por violación de FK; cerraba además una referencia cross-tenant). |
| DT-3 | Etapa 5 — front `EventoClinicoFormDialog` / `EutanasiaDialog` | **Workaround temporal:** el dropdown de profesional envía `d.userId` (opción A) y **filtra en el front** los doctores con `user_id = null` (nullable por `ON DELETE SET NULL`), que existen para Turnos/Horarios pero son inválidos como profesional de historial. La asimetría sigue en el modelo; el filtro es un parche de presentación, no una garantía. Además, si DT-1 produce doctores duplicados para un mismo usuario, `value={d.userId}` generaría dos `SelectItem` con la misma `value`. **Los diffs del workaround están sin commitear a propósito: se absorben en S4.** | Resolver junto con DT-1 (unicidad) y DT-2 (contrato). Idealmente el backend no debería devolver como "profesional seleccionable" a un doctor sin usuario. | Etapa 5 (registro de evento clínico). | **Resuelta (commit `ca59c70`, Etapa 9 — S4).** Los diffs sin commitear se absorbieron: los dos diálogos envían `d.userId` y listan con `listarDoctores({ available: true, professional: true })`, así la garantía es del backend. El `.filter(d => d.userId)` del front **sobrevive como defensa en profundidad/type-guard documentado** (ya no es la única barrera). El riesgo de `SelectItem` duplicados desapareció con el UNIQUE de DT-1. |
| DT-4 | Etapa 2 — `scripts/seed.mjs` | **Idempotencia incompleta del seeder:** en la rama "usuario ya existe" (`seed.mjs:195-198`) se reusa el usuario de Auth **sin re-setear** `password` ni `email_confirm`; si la contraseña cambió o el mail quedó sin confirmar, re-correr el seed no deja credenciales usables. | En la rama "ya existe", llamar `auth.admin.updateUserById` con `password` + `email_confirm: true` (y `app_metadata` si corresponde). | Etapa 9 — S1 (diagnóstico). | **Resuelta (commit `1faa545`, Etapa 9 — S2).** |
| DT-5 | Etapa 2 — migración `20260622000001_grants_supabase_roles.sql` | **GRANT DML demasiado amplio a `anon`:** la migración otorga `SELECT, INSERT, UPDATE, DELETE ON ALL TABLES` (+ `ALTER DEFAULT PRIVILEGES`) también al rol `anon`. RLS contiene el daño, pero viola mínimo privilegio; el comentario de la propia migración ya pide acotarlo. | Migración **nueva** que revoque DML de `anon` y deje solo `SELECT` sobre catálogos globales si el proxy de catálogos realmente entra como `anon` (verificar el rol antes del REVOKE). Test de integración: `anon` sin DML sobre tablas de negocio. | Etapa 9 — S1 (diagnóstico). | **Resuelta (Etapa 9 — S3): se verificó empíricamente que el proxy de catálogos (`web/src/api/catalogos.ts`) reenvía el JWT del usuario logueado como `Authorization: Bearer`, así que PostgREST resuelve el rol efectivo como `authenticated` (no `anon`) en todo uso real — las pantallas están detrás de `ProtectedRoute`. Migración `20260706000001_revoke_anon_grants.sql` revoca TODO privilegio de `anon` (DML + SELECT, tablas y default privileges); `authenticated`/`service_role` no se tocan. Test `tests/integration/grants.integration.test.ts` cubre 42501 para DML e SELECT de `anon` y confirma que `authenticated` sigue leyendo los catálogos; verificado además en navegador contra la pantalla real de alta de mascota.** |
| DT-6 | Etapas 5–8 — `src/shared/errors.ts` | **Nomenclatura inconsistente de códigos de error:** guardería mezcla `ESTADIA_NOT_FOUND` (español) con `STAY_OVERLAP`/`STAY_LOCKED` (inglés); vacunación usa `VACCINE_*` en inglés. No hay duplicados semánticos, pero el criterio de idioma/prefijo no es uniforme. | Decidir convención (un prefijo por módulo) y renombrar en `errors.ts` + services + front (`ApiError`) + tests en un solo PR mecánico. | Etapa 9 — S1 (diagnóstico). | **Resuelta (commit `5c961ea`, Etapa 9 — S2): el Documento Maestro/Addendum v1.1 mandan `STAY_OVERLAP`/`STAY_LOCKED` explícitamente, así que se renombró solo `ESTADIA_NOT_FOUND` → `STAY_NOT_FOUND`; `CUPO_GUARDERIA_AGOTADO` se deja igual (también mandato de la spec).** |
| DT-7 | Etapa 3 — `web/package.json` | **Vite fijado en 6.3.5** (desactualizado; advisories posteriores en la serie 6.x). | Bump a la última 6.x (o mayor si el changelog lo permite sin fricción), verificar `npm run build` + suite de componentes. | Etapa 9 — S1 (diagnóstico). | **Resuelta (commit `536dc90`, Etapa 9 — S2): bump a 6.4.3 (última de la serie 6.x), sin breaking changes.** |
| DT-8 | Etapas 5–7 — `supabase/migrations/` | **Historial de migraciones con parches encadenados:** `fix_eutanasia_ambiguity`, `fix_crear_estadia_ambiguity` y `reload_pgrst_schema` corrigen migraciones anteriores. Funciona, pero ensucia el historial de cara al primer deploy. | Antes del primer deploy (aún sin producción): decidir consolidación/squash con `supabase db reset` + suite completa como gate, o documentar el orden y dejarlo como está (opción de menor riesgo). | Etapa 9 — S1 (diagnóstico). | **Resuelta (Etapa 9 — S11): squash quirúrgico.** `20260623000002_registrar_eutanasia_rpc.sql` y `20260629000001_crear_estadia_con_cupo_rpc.sql` quedaron con la versión final de cada RPC (cuerpos verificados **byte-idénticos** a los de las migraciones-fix, vía diff contra git); se borraron las 4 correctivas (`..0003/0004/0005` de eutanasia y `..0002` de estadías). 24 → 20 archivos; las otras migraciones no se tocaron. Viable solo pre-producción. Gate cumplido: `supabase db reset` (20 migraciones limpias) + seed + typecheck + unit 417 + integración 146 + web 286 + e2e 18. El detalle iterativo sobrevive en el historial de git. |
| DT-9 | Etapa 4 — RN-AUD4 | **Política de retención de auditoría no implementada:** RN-AUD4 pide conservar histórico según política (N registros / X meses); hoy no hay job ni límite de purga. Los índices por fecha/módulo se verifican en S10. | Decidir si entra al MVP (job de purga o particionado simple) o queda post-MVP documentado. | Etapa 9 — S1 (diagnóstico). | Etapa 9 — S11 (decisión) |
| DT-10 | Etapa 9 — S6 — `20260706000003_cron_notificaciones.sql` | **Secrets del cron de notificaciones a setear en deploy:** el job pg_cron/pg_net queda inerte hasta cargar por entorno el env `CRON_SECRET` del Edge Function **y** los secrets de Vault `cron_notif_secret` (== `CRON_SECRET`) y `cron_notif_url` (URL completa del endpoint interno; ojo: local usa `/functions/v1/api/v1/...`, prod `/functions/v1/api/api/v1/...`). Sin ellos la función `disparar_notificaciones()` hace `RAISE WARNING` y no dispara. | Agregar los 3 secrets a `docs/DEPLOY.md` (checklist de secrets por entorno) y setearlos en Supabase (Dashboard → Vault + Edge Function secrets). Ver el comentario de setup al pie de la migración. | Etapa 9 — S6. | Etapa 9 — S11 (deploy) |

**Resueltos / verificados en Etapa 9 — S1 (2026-07-06):** el envío real de resumen de historial (RN-EC9) ya está implementado con `CanalEmailResend` en `historial.service.ts` (la línea de deuda que figuraba acá era obsoleta y se eliminó); no quedan `console.log` de debug en ninguna suite de tests. Matriz completa de cobertura en `docs/MATRIZ_RN_TESTS.md`.